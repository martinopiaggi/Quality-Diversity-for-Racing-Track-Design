{
    "scripts": {
        "start": "node server.js",
        "build": "node generateTrackIndex.js && cp -R ../src ./src && cp -R ../data ./data"
    },
    "dependencies": {
        "express": "^4.19.2",
        "serverless-http": "^3.2.0"
    }
}
